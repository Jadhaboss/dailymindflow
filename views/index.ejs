<div class="hero">
    <h1>Flow Your Mind Daily.</h1>
    <p>Discover stories, thinking, and expertise from writers on any topic. A place where ideas find their voice.</p>
</div>

<section class="posts-section container">
    <div class="post-grid">
        <% data.forEach(post=> { %>
            <article class="post-card reveal">
                <a href="/post/<%= post._id %>">
                    <div class="card-image-wrapper">
                        <% if(post.image) { %>
                            <img src="<%= post.image %>" alt="<%= post.title %>" loading="lazy">
                            <% } else { %>
                                <div class="placeholder-image" style="width:100%; height:100%; background: #eee;"></div>
                                <% } %>
                    </div>
                    <div class="post-content">
                        <h3>
                            <%= post.title %>
                        </h3>
                        <p class="post-excerpt">
                            <%= post.body.substring(0, 120) %>...
                        </p>
                        <span class="read-more-btn">Read More <i class="fas fa-arrow-right"></i></span>
                    </div>
                </a>
            </article>
            <% }) %>
    </div>

    <% if (nextPage !==null) { %>
        <div class="pagination reveal">
            <a href="/?page=<%= nextPage %>" class="btn btn-primary">Load Older Posts</a>
        </div>
        <% } %>
</section>